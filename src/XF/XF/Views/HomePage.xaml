<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:XF.ViewModels;assembly:ViewModels"
             xmlns:control="clr-namespace:XF.Controls;assembly:Controls"
             x:Class="XF.Views.HomePage"
             NavigationPage.HasNavigationBar="false">
    <ContentPage.BindingContext>
        <vm:HomePageViewModel/>
    </ContentPage.BindingContext>
    
    <ContentPage.Content>
        <Grid VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand" RowSpacing="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>
            <control:Header Title="{Binding HeaderTitle}"
                            RIconSource="{Binding HeaderRightIconSource}"
                            OnRIconClick="{Binding HeaderRightCommand}"
                            Grid.Row="0"/>
        </Grid>
    </ContentPage.Content>
</ContentPage>
